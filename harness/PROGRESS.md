# 진행 상황

갱신: 2026-09-08 (주전 명단·전투 위치 번호 갱신)

## 현재 상태

- 브랜치 `main`, 워킹트리 clean, `origin/main`(github.com/formars0309-cloud/wb-row-league)과 동기화됨.
- 원본 이력 이전 완료. 조율자 확인 기준으로 lint·build·tests 모두 통과 상태.
- 최신 작업 — 2026-09-08 게임 스크린샷 기준으로 전투 위치 번호 30개와 주전 명단을 갱신. 주전/예비는 이제 `SLOT_SOURCE`(번호표) 하나에서 파생되며 `RESERVE_PLAYERS`는 삭제됨.

## 마지막으로 한 일

- 벌꿀오소리 = 마법공주간달프(닉네임 변경)로 확인돼 로스터를 하나로 합침. `mergePlayers`의 RENAMED가 저장본의 옛 이름을 옮긴다. 늑대장군은 남쪽 기병대라 기병으로. MAHA 19·산삼맨 25로 맞바꿈.
- 로스터·번호표·임무 브리프를 `app/roster.ts` 한 파일로 모음(단일 출처). 작전판 카드·폰 화면·PNG 카드(`cards/build.mjs`, Node 24 타입 스트리핑으로 .ts를 직접 import)가 같이 읽는다. 지휘부 오더(2026-09-08)로 30인 브리프를 넣고 PNG 30장 생성. 지휘 보직(RALLY/GARRISON/BLOCKER)도 오더대로 갱신, 저장본은 로드 때 다시 계산. 임무 파서는 거점 이름(천무·축복·군왕·목명·용기)을 시계보다 먼저 보고, 막기를 집결보다 먼저 본다.
- MAHA 25·산삼맨 19가 최종(지휘부 확인). 오더 원문의 "19. MAHA"는 오기. TOMAS SHELBY 병종을 기병으로 바꾸고 임무표에 기병대 5부대 행 추가.
- 전투 위치 번호 재배치(2026-09-08). 신규 3명(마법공주간달프·TOMAS SHELBY·보 수)은 임무표 시트에 없어 병종을 보병으로 가정해 넣음 — 시트가 갱신되면 `PLAYER_SOURCE`·`MISSION_SOURCE` 재확인 필요. 벌꿀오소리·최산수·마 젤 란·대장군 뽀로링은 번호가 빠져 예비로 내려감(임무표의 "오소리님 집결 탑승"은 그대로라 시트 갱신 시 함께 볼 것).
- 저장본(localStorage)의 주전/예비는 로드 때 현재 번호표로 다시 계산한다. UI의 주전/예비 토글은 세션 안에서만 유지됨.
- 테스트 카운트 35/31/4 → 41/30/11로 갱신. 갱신 전 테스트는 이미 실패 상태였음(소스 38명·예비 8명 vs 테스트 35명·예비 4명).

- 팀원용 폰 화면 추가(02654a0), 전투 위치 번호 일괄 배치(6e0c7b6), 보드 대칭화(75ffbf3),
  입구 막기 명령의 목적지를 적 진영 입구로 교정(81189d6) 등 작전판 기능 마무리.
- 이 하네스 폴더(`harness/`) 신설. 코드에는 손대지 않음.

## 다음 세션이 할 일

1. `npm test`와 `npm run lint`를 돌려 기존 기능이 깨지지 않았는지 확인한다.
2. 임무 문안이 바뀌면 `app/roster.ts`의 MISSION_BRIEFS만 고치고 `node cards/build.mjs`로 PNG를 다시 뽑는다. 구글 시트는 더 이상 임무의 출처가 아니다.
3. `FEATURES.md`의 남은 단위가 비어 있으므로, 새 일감은 조율자가 추가할 때까지 착수하지
   않는다. 임의로 기능을 추가하지 말 것.
4. 참고: 폰 화면(02654a0)은 전체 테스트는 통과하지만 전용 단언이 없다. 조율자가
   테스트 보강을 일감으로 올릴 수 있음.

## 테스트·실행 방법

- 개발 서버: `npm run dev`
- 빌드: `npm run build` (vinext build, 산출물 `dist/`)
- 테스트: `npm test` — 빌드 후 `tests/rendered-html.test.mjs`를 node --test로 실행.
  dist의 서버 렌더 HTML과 소스 패턴을 함께 단언한다.
- 린트: `npm run lint`
- 요구 환경: Node.js >= 22.13.0

## 주의

- 미확인 가정(지휘부 답 대기): 남쪽 보병 3인의 "6시시 치료의 영목"은 6시 용기의 영목으로 적음. 테슬라의 "12시 천무전당"은 원문 유지(경로는 이름으로 1시 천무에 연결). 병종 등록과 임무가 다른 사람(곡곡이·불개·SIGH·Kingsway·5000·TESLA)은 등록 병종 유지. 서틸로/떡틸로, 햄찌/햄수 표기 차이 있음.

- 상태 저장은 브라우저 localStorage 키 `heinapel-war-table-v0.3`. 스키마를 바꾸면
  `mergePlayers`·`normalizeScene` 마이그레이션 경로와 STORAGE_KEY 버전을 함께 고려할 것.
- 테스트가 소스 문자열 패턴(선수 명단, 상수명)까지 단언하므로 리팩터 시 테스트도 함께
  갱신해야 한다. 단, 기준 완화는 조율자 승인 필요.
