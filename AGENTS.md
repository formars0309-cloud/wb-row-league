# WB ROW League — 에이전트 지침

## 하네스 엔지니어링

에이전트 세션은 컨텍스트가 끊기면 이전 일을 기억하지 못한다. 그래서 **상태는 대화가
아니라 파일에 남긴다.** 이 저장소의 `harness/` 폴더가 그 자리다.

- `harness/FEATURES.md` — 작업 단위 목록. 항목마다 **검증 기준**(테스트 명령 또는
  확인 절차)과 통과 여부(`[ ]`/`[x]`). 검증 기준은 조율자만 고친다. **워커는 기준을
  삭제·완화할 수 없다.**
- `harness/PROGRESS.md` — 직전 세션이 무엇을 했고 다음에 뭘 해야 하는지. 새 세션이
  가장 먼저 읽는 파일. 세션을 마칠 때마다 갱신한다.

### 세션 프로토콜

1. **시작** — `git log --oneline -10`과 `harness/PROGRESS.md`를 읽고, 실행·테스트
   수단이 있으면 돌려 기존 기능이 깨지지 않았는지 먼저 확인한다.
2. **선택** — `FEATURES.md`에서 미완 항목 **하나만** 고른다. 한 세션에 한 단위.
3. **검증** — 그 항목의 검증 기준을 **실제로 실행해 통과**해야 완료다. 테스트를
   지우거나 기준을 바꿔서 통과시키는 것은 금지.
4. **마무리** — 커밋(한국어 메시지)하고 `PROGRESS.md`를 갱신한다. 검증이 안 끝난
   중간 상태는 완료로 표시하지 않는다.

완료 주장은 검증 기준 재실행으로 확인한 뒤에만 `[x]` 처리한다. 커밋 이력이 곧 복구
지점이다 — 세션이 망가뜨린 게 있으면 마지막 정상 커밋으로 되돌리고 `PROGRESS.md`에
실패 내역을 남긴다.

## 미니멀 코딩 — 포니테일

Claude Code와 Codex 전역에 [ponytail](https://github.com/DietrichGebert/ponytail)
플러그인이 설치돼 있다. 코드를 쓰기 전에 사다리(필요한가 → 코드베이스에 있나 →
표준 라이브러리 → 네이티브 기능 → 기존 의존성 → 한 줄 → 최소 코드)를 타게 하는
규칙으로, 본문은 플러그인이 세션마다 주입하므로 여기 반복하지 않는다.
이 저장소에서 그 위에 지키는 것:

- **명세에 최소 구현을 명시한다.** 워커에게 넘기는 명세에 "요청된 것만, 외과적
  수정으로"를 포함한다. 단, 진단을 건너뛰고 diff만 작게 만드는 것은 게으름이
  아니라 오답이다 — 문제를 다 읽은 뒤에 게을러진다.
- **검수에 과잉 구현 검토를 넣는다.** 검증 기준 재실행 다음에 diff 크기가 과제
  대비 정당한지 본다. 요청 없는 추상화·설정·보일러플레이트가 보이면 반려한다.
- **최소화 금지 대상.** lint·build·테스트 게이트(`npm run lint`, `npm run build`,
  `npm test` — 빌드 후 `tests/rendered-html.test.mjs` 실행),
  신뢰 경계의 입력 검증, 데이터 손실을 막는 오류 처리,
  `harness/FEATURES.md`의 검증 기준과 테스트는 "불필요"로 판정하지 않는다.
  워커가 최소화를 명분으로 테스트를 깎았으면 검수에서 반려한다.
- 강도 기본은 full. 조절은 `/ponytail lite|full|ultra`, 해제는 "stop ponytail".
